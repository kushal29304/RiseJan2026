USE A1
CREATE TABLE Trains  (
    INT train_id NOT NULL,
    train_name (,
    source (not blank),
    destination (not blank),
    departure_time (not blank),
    arrival_time (not blank),
    distance_km (should be > 0)